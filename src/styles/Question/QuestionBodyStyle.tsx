import { makeStyles } from '@material-ui/core';
import QuestionBody from '../../components/QuestionPage/components/QuestionBody';

/**
 * @see QuestionBody
 */
export const useStyles = makeStyles({
    root: {
        display: 'flex',
        flexDirection: 'column',
        width: '100%',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',
    },
});
