import { makeStyles } from '@material-ui/core';
import QuestionPagination from '../../components/QuestionPage/components/QuestionPagination';

/**
 * @see QuestionPagination
 */
export const useStyles = makeStyles({
    root: {
        width: '80%',
    },
});
